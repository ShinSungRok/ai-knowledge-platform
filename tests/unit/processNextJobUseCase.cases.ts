/** Unit cases for ProcessNextJobUseCase. */
export const PROCESS_NEXT_JOB_USE_CASE_UNIT_CASES = [
  "depends_only_on_job_processor_port",
  "execute_delegates_to_process_next",
  "invalid_input_rejected",
] as const;
