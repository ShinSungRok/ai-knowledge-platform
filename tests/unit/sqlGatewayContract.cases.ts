/**
 * Unit-level cases for `SqlGateway` contract.
 *
 * Executed via:
 *
 *   pnpm validate:infra:sql-gateway-contract
 */
export const SQL_GATEWAY_CONTRACT_UNIT_CASES = [
  "module_constant",
  "port_implementable",
  "result_shape",
  "barrel_re_exports",
] as const;
