/**
 * Skeleton module: `app/knowledge/evaluation`
 *
 * Quality, regression, and benchmark evaluation framework.
 *
 * Feature implementation is intentionally deferred. This barrel exists so the
 * module boundary is importable and validatable from day one.
 */
export const KNOWLEDGE_MODULE_EVALUATION = "app/knowledge/evaluation" as const;
