/**
 * Skeleton module: `app/knowledge/http`
 *
 * Framework-independent HTTP abstraction.
 *
 * Feature implementation is intentionally deferred. This barrel exists so the
 * module boundary is importable and validatable from day one.
 */
export const KNOWLEDGE_MODULE_HTTP = "app/knowledge/http" as const;
