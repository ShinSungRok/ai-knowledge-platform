/**
 * Skeleton module: `app/knowledge/embedding`
 *
 * Chunking, embedding, and vector indexing ports/adapters.
 *
 * Feature implementation is intentionally deferred. This barrel exists so the
 * module boundary is importable and validatable from day one.
 */
export const KNOWLEDGE_MODULE_EMBEDDING = "app/knowledge/embedding" as const;
