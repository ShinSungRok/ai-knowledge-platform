/**
 * Skeleton module: `app/knowledge/persistence`
 *
 * Concrete repository adapters (JSON, PostgreSQL, etc.).
 *
 * Feature implementation is intentionally deferred. This barrel exists so the
 * module boundary is importable and validatable from day one.
 */
export const KNOWLEDGE_MODULE_PERSISTENCE = "app/knowledge/persistence" as const;
