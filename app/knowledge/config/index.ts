/**
 * Skeleton module: `app/knowledge/config`
 *
 * Typed, validated, environment-driven application configuration.
 *
 * Feature implementation is intentionally deferred. This barrel exists so the
 * module boundary is importable and validatable from day one.
 */
export const KNOWLEDGE_MODULE_CONFIG = "app/knowledge/config" as const;
