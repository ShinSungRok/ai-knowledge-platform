/**
 * Skeleton module: `app/knowledge/security`
 *
 * Rate limiting and input validation foundations.
 *
 * Feature implementation is intentionally deferred. This barrel exists so the
 * module boundary is importable and validatable from day one.
 */
export const KNOWLEDGE_MODULE_SECURITY = "app/knowledge/security" as const;
