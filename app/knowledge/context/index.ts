/**
 * Skeleton module: `app/knowledge/context`
 *
 * Prompt context assembly from retrieved documents.
 *
 * Feature implementation is intentionally deferred. This barrel exists so the
 * module boundary is importable and validatable from day one.
 */
export const KNOWLEDGE_MODULE_CONTEXT = "app/knowledge/context" as const;
