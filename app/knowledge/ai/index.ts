/**
 * Skeleton module: `app/knowledge/ai`
 *
 * AI provider abstraction (fake + real providers).
 *
 * Feature implementation is intentionally deferred. This barrel exists so the
 * module boundary is importable and validatable from day one.
 */
export const KNOWLEDGE_MODULE_AI = "app/knowledge/ai" as const;
