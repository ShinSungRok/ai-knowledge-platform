/**
 * Skeleton module: `app/knowledge/pipeline`
 *
 * Ingestion pipelines from external knowledge sources.
 *
 * Feature implementation is intentionally deferred. This barrel exists so the
 * module boundary is importable and validatable from day one.
 */
export const KNOWLEDGE_MODULE_PIPELINE = "app/knowledge/pipeline" as const;
