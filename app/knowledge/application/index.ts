/**
 * Skeleton module: `app/knowledge/application`
 *
 * Use-case orchestration over domain types and ports.
 *
 * Feature implementation is intentionally deferred. This barrel exists so the
 * module boundary is importable and validatable from day one.
 */
export const KNOWLEDGE_MODULE_APPLICATION = "app/knowledge/application" as const;
