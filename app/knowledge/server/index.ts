/**
 * Skeleton module: `app/knowledge/server`
 *
 * Production server runtime and lifecycle.
 *
 * Feature implementation is intentionally deferred. This barrel exists so the
 * module boundary is importable and validatable from day one.
 */
export const KNOWLEDGE_MODULE_SERVER = "app/knowledge/server" as const;
