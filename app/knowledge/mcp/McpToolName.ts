/**
 * Known MCP tool names exposed by this platform's capability boundary.
 *
 * Currently a single tool: generate a workspace-scoped cited grounded
 * answer. Additional tool names are later tasks.
 */
export type McpToolName = "generate_cited_grounded_answer";
